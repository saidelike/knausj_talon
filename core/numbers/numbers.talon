not tag: user.mouse_grid_showing
-
# disable numbers without saying "numb" in front to avoid false positives
# <user.number_string>: "{number_string}"
# and only support something like "numb fifty five point two" -> 55.2
<user.number_prefix>:       "{number_prefix}"
